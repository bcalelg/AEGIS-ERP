import { provideHttpClient } from '@angular/common/http';
import { HttpTestingController, provideHttpClientTesting } from '@angular/common/http/testing';
import { TestBed } from '@angular/core/testing';
import { DocumentoPersonaService } from './documento-persona.service';
describe('DocumentoPersonaService',()=>{let service:DocumentoPersonaService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(DocumentoPersonaService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('uses both composite key parts on update',()=>{const body={tipoDocumentoId:1,personaId:2,numeroDocumento:'ABC'};service.update(1,2,body).subscribe();const request=http.expectOne('/api/planilla/documentos-persona/1/2');expect(request.request.method).toBe('PUT');expect(request.request.body).toEqual(body);request.flush({});});it('searches persona options without loading all manually',()=>{service.personas('Ana').subscribe();const request=http.expectOne(r=>r.url.endsWith('/options/personas')&&r.params.get('search')==='Ana');expect(request.request.method).toBe('GET');request.flush([]);});});
