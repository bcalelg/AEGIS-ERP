import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{TestBed}from'@angular/core/testing';import{EmpleadoService}from'./empleado.service';
describe('EmpleadoService',()=>{let service:EmpleadoService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(EmpleadoService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('consulta empleados con búsqueda',()=>{service.list('Ana').subscribe();const r=http.expectOne(x=>x.url==='/api/planilla/empleados'&&x.params.get('search')==='Ana');expect(r.request.method).toBe('GET');r.flush([]);});it('obtiene status permitidos desde backend',()=>{service.statusPermitidos(9).subscribe();const r=http.expectOne('/api/planilla/empleados/9/status-permitidos');expect(r.request.method).toBe('GET');r.flush([]);});it('busca personas y exporta Excel como blob',()=>{service.personas('López').subscribe();http.expectOne(x=>x.url.endsWith('/options/personas')&&x.params.get('search')==='López').flush([]);service.export('excel').subscribe();const r=http.expectOne('/api/planilla/empleados/export/excel');expect(r.request.responseType).toBe('blob');r.flush(new Blob());});});
